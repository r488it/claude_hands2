## Setup Instructions
```bash
docker build --platform linux/amd64 -t claude_hands_2 .
docker build -t claude_hands_2 .
docker-compose up -d

docker exec -it claude_hands2-agent-1 bash  
./install.sh

claude 
>login
```


## test post slack
uv run agent.py "自己紹介して、にSlackに投稿して"

# run server
node claude_hands2.js


uv run agent.py "Slack一覧を取得して"
